{
	"type": "object",
	"defaultProperties": [],
	"additionalProperties": false,
	"properties": {
		"starting_script": {
			"title": "starting_script",
			"description": "Plays this script at the start of the pattern. This is only played once. If the starting script is empty, it will move over to the main script.",
			"type": "array",
			"items": {
				"$ref": "ability_pattern_action.json"
			}
		},
		"script": {
			"title": "script",
			"description": "Plays this script on loop.",
			"type": "array",
			"items": {
				"$ref": "ability_pattern_action.json"
			}
		}
	}
}