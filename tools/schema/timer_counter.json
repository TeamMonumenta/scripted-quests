{
    "type": "object",
    "additionalProperties": false,
    "properties": {
	    "world": {
		    "required": true,
			"description": "The world for the counter to appear in.",
            "type": "string",
            "propertyOrder": 4,
			"default": "world"
		},
        "x": {
            "required": true,
            "$ref": "coordinate.json",
            "propertyOrder": 1
        },
        "y": {
            "required": true,
            "$ref": "coordinate.json",
            "propertyOrder": 2
        },
        "z": {
            "required": true,
            "$ref": "coordinate.json",
            "propertyOrder": 3
		},
		"timer_tag": {
		    "required": true,
			"description": "The timer tag required for this counter to be shown.",
            "type": "string",
            "propertyOrder": 4,
			"default": "world"
		},
        "message": {
            "required": true,
			"description": "The message to display on the counter. Use @T within the message to display the time.",
            "type": "string",
            "propertyOrder": 4
		}
    }
}
