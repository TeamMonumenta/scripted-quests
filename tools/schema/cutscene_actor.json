{
  "type": "object",
  "patternProperties": {
    ".*": {
		"headerTemplate": "{{i}}",
		"description": "An actor variable, with the JSON key being the variable ID/name, and the specified object with the JSON key being information about the actual actor.",
		"oneOf": [
			{
				"title": "Actor ID",
				"type": "string"
			},
			{
				"title": "Player",
				"type": "object",
				"properties": {
					"viewer": {
						"title": "viewer",
						"description": "Toggles whether or not this actor is the watcher of this cutscene, making it so it uses their player character as an actor. If this option is toggled on, all other fields below are ignored.",
						"type": "boolean",
						"default": true
					}
				}
			}
		]
	}
  }
}