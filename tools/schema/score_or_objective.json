{
    "options": {
        "keep_oneof_values": false
    },
    "oneOf": [
        {
            "title": "Constant",
            "$ref": "score.json"
        },
        {
            "title": "Objective",
            "type": "string",
            "pattern": "^[-+._A-Za-z0-9]+$"
        }
    ]
}
